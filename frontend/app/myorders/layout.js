export const metadata = {
    title: 'My Orders | Forever Trendin',
    description: 'Forever Trendin is a fashion store that sells the latest fashion trends.',
}

export default function MyOrdersLayout({ children }) {
    return (
        <div>
            {children}
        </div>
    )
}